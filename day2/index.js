// function well(x) {
//   const n = x.filter(v => v === "good").length;
//   return n === 0 ? "Fail!" : n <= 2 ? "Publish!" : "I smell a series!";
// }

// function stringToNumber(str) {
//   return Number(str);
// }